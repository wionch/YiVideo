## 1. Implementation
- [x] 1.1 审阅 `SINGLE_TASK_API_REFERENCE.md` 通用部分，与 `SingleTaskRequest`/`TaskStatusResponse`/`CallbackResult`/`WorkflowContext` 实际字段对照，列出差异清单。
- [x] 1.2 校准 `/v1/tasks` 通用示例：状态/结果/回调载荷字段（含 `status`/`updated_at`/`minio_files`/`callback_status` 等）及时间/URL占位符。
- [x] 1.3 节点级校验（请求体参数对齐 + WorkflowContext/响应示例字段对齐，依次完成）：
  - [x] ffmpeg.extract_keyframes（参数/输出补齐压缩统计）
  - [x] ffmpeg.extract_audio
  - [x] ffmpeg.crop_subtitle_images
  - [x] ffmpeg.split_audio_segments
  - [x] faster_whisper.transcribe_audio
  - [x] audio_separator.separate_vocals（参数/输出补齐 MinIO URL、模型信息）
  - [x] pyannote_audio.diarize_speakers
  - [x] paddleocr.detect_subtitle_area
  - [x] paddleocr.create_stitched_images（新增节点段落）
  - [x] paddleocr.perform_ocr
  - [x] indextts.generate_speech
  - [x] wservice.generate_subtitle_files
  - [x] wservice.correct_subtitles
  - [x] wservice.ai_optimize_subtitles
- [x] 1.4 校对 `/v1/files` 端点请求/响应示例与 `FileOperationResponse`/`FileUploadResponse` 实际返回字段，补充安全约束说明。
- [x] 1.5 与 `WORKFLOW_NODES_REFERENCE.md` 交叉验证剩余节点字段命名与默认值，确保两处一致。
- [x] 1.6 运行 `openspec validate validate-single-task-docs --strict`，确保变更通过校验（更新完所有节点后重跑）。
