# IndexTTS Service Requirements
# 基于 IndexTTS2 模型的文本转语音服务
# 注意：这些依赖将在 IndexTTS2 的 uv 虚拟环境中安装

# ========================================
# 核心框架依赖
# ========================================
celery>=5.3.0
redis>=5.0.0
pydantic>=2.0.0
PyYAML>=6.0

# ========================================
# 系统工具库
# ========================================
psutil>=5.9.0                    # 系统监控
requests>=2.31.0                 # 网络请求
tqdm>=4.65.0                     # 进度条显示
pathlib2>=2.3.7                  # 文件处理 (Python 2 兼容)
ffmpeg-python>=0.2.0             # 音频编码和格式转换
colorlog>=6.0.0    
gradio              
huggingface-hub              
deepspeed
# ========================================
# 注意
# ========================================
# IndexTTS2 的核心依赖 (torch, transformers, librosa 等)
# 已通过 uv sync 在 Dockerfile 中安装
# 此文件仅包含 Celery 工作节点需要的额外依赖