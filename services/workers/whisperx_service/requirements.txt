# WhisperX Service Requirements - 优化版本 V2
# 基础框架依赖（在 Dockerfile 中已直接安装）
celery>=5.3.0
redis>=5.0.0
pydantic>=2.0.0
PyYAML>=6.0

# 音频处理依赖
ffmpeg-python>=0.2.0

# Faster-Whisper 优化依赖（在 Dockerfile 中已直接安装）
faster-whisper>=1.0.0
ctranslate2>=4.0.0

# 其他 WhisperX 依赖（在 Dockerfile 中已直接安装）
pyannote.audio>=3.0.0

# WhisperX 核心（在 Dockerfile 中已直接安装）
# git+https://github.com/m-bain/whisperx.git

# 可选：性能优化依赖
# 注意：以下依赖根据实际需要安装，以减少镜像大小
# torch>=2.1.0  # 基础镜像已包含
# torchaudio>=2.1.0  # 基础镜像已包含